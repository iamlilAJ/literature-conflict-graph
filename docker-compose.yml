services:
  aigraph:
    build: .
    image: aigraph:latest
    container_name: aigraph
    restart: unless-stopped
    ports:
      - "7860:7860"
    env_file:
      - .env
    environment:
      AIGRAPH_EXTRACT_CONCURRENCY: "2"
    volumes:
      - ./outputs:/app/outputs
